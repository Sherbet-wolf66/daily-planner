[project]
name = "daily-planner"
version = "0.1.0"
description = ""
authors = [
    {name = "Sherbet-wolf66" ,email = "punkysue08@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "daily_planner", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
